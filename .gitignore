node_modules
.next
apps/web/test-results/
.env
.env.local
.DS_Store
*.log
dist
.turbo
*.tsbuildinfo

# Agent scratch/credential files
.codex-*
# Plaintext credential dumps — never commit
DANIEL_ENV.txt
*_ENV.txt
*.secrets
.env.*.local

# Agent scratch scripts — ad-hoc probes, never part of the build.
*.tmp.mjs
