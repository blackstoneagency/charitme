{
  "name": "money-raise",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/web",
    "build": "npm run build --workspace=apps/web",
    "typecheck": "npm run typecheck --workspace=apps/web",
    "schema:snapshot": "node scripts/schema-snapshot.mjs",
    "test:seed-guard": "node --test scripts/seed-guard.test.mjs",
    "test:rls-live": "node scripts/rls-live-smoke.mjs",
    "provision": "node scripts/provision-supabase-vercel.mjs"
  },
  "devDependencies": {
    "supabase": "^2.101.0",
    "vercel": "^54.4.1"
  },
  "overrides": {
    "postcss": "8.5.23",
    "sharp": "0.35.3"
  }
}
